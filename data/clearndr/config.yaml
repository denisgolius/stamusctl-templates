includes:
  ./sub-config/db.config.yaml
  ./sub-config/opensearch.config.yaml
  ./sub-config/suricata.config.yaml
  ./sub-config/scirius.config.yaml
  ./sub-config/utils.config.yaml

globals:
  restartmode:
    type: string
    usage: Restart mode for all services
    default: unless-stopped
    choices: restart
